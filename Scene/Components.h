#pragma once
#include "../main/LinaMath.h"
namespace Lina{
    struct idComponent
    {
        Random Id;
        IdComponent() = default();
        IdComponent(const IdComponent&) = default();
    }
}
